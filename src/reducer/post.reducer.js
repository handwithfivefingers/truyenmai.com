const initState = {
    post: [],
}

export default (state = initState, action) => {
    switch (action.type) {
        case value:

            break;

        default:
            state = {
                ...state
            }
            break;
    }
}